# Deny direct access to uploaded files
<Files "*">
    Order Allow,Deny
    Deny from all
</Files>

# Allow only specific file types if needed
<FilesMatch "\.(csv|txt)$">
    Order Allow,Deny  
    Allow from all
</FilesMatch>
